# MSG2100-UPON-AC 刷 OpenWrt 指南

## 固件产物（已生成，在 firmware/ 目录）

| 文件 | 说明 |
|---|---|
| `openwrt-econet-en7528-raisecom_msg2100-upon-ac-squashfs-tclinux.trx` (8MB) | **刷机镜像**（HDR2 头 + LZMA 内核 + UBI rootfs，decompAddr=0x80002000） |
| `openwrt-econet-en7528-raisecom_msg2100-upon-ac-squashfs-sysupgrade.bin` (7.26MB) | sysupgrade 格式（OpenWrt 内升级用） |

## ⚠️ 重要前提

1. 本机是**纯 PON 上联**，OpenWrt 主线暂无可用 GPON 驱动 → **OpenWrt 无法通过光纤上网**（至少现在不行）
2. 刷机用**双分区**：原厂固件在 mtd4，OpenWrt 刷到 **mtd7（slave 备份分区）**，随时可切回
3. **必须有 USB-TTL 串口兜底**（已接好），出问题可进 bootloader/root shell 恢复

## 刷机步骤（root shell 直写 mtd7，最安全）

### 1. 把固件放到设备可访问处
- 用 HTTP：把 `.trx` 放到 Mac 任意 HTTP 服务器，或
- 直接在设备 root shell 里用网络下载，或
- 简单法：分块 dd 进 /www/tmp（见之前的实战方法）

### 2. 获取 root shell（可重复的方法）
```
telnet 192.168.1.1   →  user / 499irLhP  →  enable
openconfig json-parse /dev/mtd13    ← 触发 CLI 崩溃 → 掉进 # busybox root shell
```

### 3. 写 mtd7（slave 分区）
```sh
# 假设 .trx 已在设备 /www/tmp/openwrt.trx（或 /tmp/）
dd if=/www/tmp/openwrt.trx of=/dev/mtd7
sync
```

### 4. 切换到 slave 启动
从 root shell 或 bootloader 设置启动标志切到 slave 分区，重启。

（注：具体切换方法需确认 bootloader 的 boot flag 设置方式——本机 bootloader 有 "==> boot flag = 0" 输出，flag=0 主分区 / flag=1 可能为 slave。建议先在 root shell 里 dump 出当前 flag 再改。）

### 5. 串口观察 OpenWrt 启动
重启后串口应显示 OpenWrt 启动日志（Linux 6.18 + OpenWrt）。首次启动后：
- LAN 口 192.168.1.1，SSH/telnet 无密码（OpenWrt 默认）
- 若启动失败，切回 mtd4（原厂固件完好）

## 回滚方法
- 若 OpenWrt 起不来：重新进 bootloader/root shell，把 boot flag 切回 mtd4，重启即回原厂

## 已知限制
- GPON 上网：暂不可用（econet-xpon 驱动在 [PR #24577](https://github.com/openwrt/openwrt/pull/24577)，未合并主线）
- LED GPIO：DTS 里已移除（引脚未实机验证），后续可补充
- 本指南的"boot flag 切换"具体命令需结合 bootloader 实测确定

## 下一步（可选，后续）
- 联系 OpenWrt 论坛 STRIJ（EN7528HU 已跑通者）交流 GPON 驱动
- 补 LED/按键 GPIO 到 DTS
- 尝试 econet-xpon PR 让 GPON 可用
